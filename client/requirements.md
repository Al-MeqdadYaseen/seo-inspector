## Packages
framer-motion | Smooth animations for result cards and transitions
lucide-react | Beautiful icons for the interface

## Notes
API endpoints:
- POST /api/analyze - Accepts { url: string }
- GET /api/history - Returns list of past analyses
